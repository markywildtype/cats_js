const app = function(name, food, image){

  const name = document.createElement('li');
  name.innerText = name;

  const food = document.createElement('li')
  food.innerText = food;

}


window.document.addEventListener("DOMContentLoaded", app);
